overwrite: true
schema: "${GRAPHCMSURL}/${GRAPHCMSPROJECT}/${BRANCH}"
documents: graphql/**/*.ts
generates:
  generated/apollo-components.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
